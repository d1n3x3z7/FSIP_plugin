name: FSIP_MCPlugin
version: '${project.version}'
main: me.d1n3x3z7.fsip_mcplugin.FSIP_MCPlugin
api-version: '1.20'
prefix: FSIP
load: POSTWORLD
authors: [d2n4x7z]
description: A plugin that opens a static address for joining
website: bio.d1n3x3z7.ru

commands:
  fsip:
    usage: /fsip
    aliases: ['/fsip']
    description: A plugin that opens a static address for joining
    permission: fsip.op

permissions:
  fsip.op:
    default: op